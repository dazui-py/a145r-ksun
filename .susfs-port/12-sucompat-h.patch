diff --git a/kernel-6.6/KernelSU-Next/kernel/sucompat.h b/kernel-6.6/KernelSU-Next/kernel/sucompat.h
index 1afba60f..ebee691b 100644
--- a/kernel-6.6/KernelSU-Next/kernel/sucompat.h
+++ b/kernel-6.6/KernelSU-Next/kernel/sucompat.h
@@ -10,7 +10,11 @@ void ksu_sucompat_exit(void);
 
 // Handler functions exported for hook_manager
 int ksu_handle_faccessat(int *dfd, const char __user **filename_user, int *mode, int *__unused_flags);
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 1, 0) && defined(CONFIG_KSU_SUSFS)
+int ksu_handle_stat(int *dfd, struct filename **filename, int *flags);
+#else
 int ksu_handle_stat(int *dfd, const char __user **filename_user, int *flags);
+#endif // #if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 1, 0) && defined(CONFIG_KSU_SUSFS)
 long ksu_handle_execve_sucompat(const char __user **filename_user, int orig_nr, const struct pt_regs *regs);
 
 #endif
\ No newline at end of file
